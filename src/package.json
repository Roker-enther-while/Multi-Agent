{
  "name": "multi-agent-ai-core",
  "version": "0.1.0",
  "description": "Multi-Agent AI workflow contracts and artifact foundation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "multi-agent-workflow": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "node --test dist/**/*.test.js"
  },
  "keywords": ["multi-agent", "ai", "workflow", "typescript"],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
